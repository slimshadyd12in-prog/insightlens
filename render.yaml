services:
  - type: web
    name: insightlens
    env: python
    plan: free
    region: oregon
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python app.py
